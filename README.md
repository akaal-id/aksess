# Cara Run dan Deploy
**Requirement:**  Node.js v.22+


1. Run perintah ini di terminal vscode:
   `npm install`
   jika terdapat error masukan perintah:
   `npm audit fix --force`
2. Run the app:
   `npm run dev`
